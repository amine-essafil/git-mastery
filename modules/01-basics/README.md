# M01 — Git Basics

## 🎯 Goal
Build strong habits with the core Git workflow:

edit → stage → commit → inspect

Understand how changes move through:
working directory → staging area → local repository

---

## 🧠 Commands Covered

- `git init` — initialize a repository
- `git status` — check current state (used constantly)
- `git add <file>` — stage a specific file
- `git add .` — stage all changes
- `git add -p` — stage changes interactively (chunk by chunk)
- `git commit -m "message"` — create a snapshot
- `git log --oneline` — view compact history
- `git log --oneline --graph --all` — visualize history
- `git diff` — see unstaged changes
- `git diff --staged` — see staged changes
- `git commit --amend` — modify the last commit

---

## 🧪 Exercises

- [ ] Exercise 01 — Make 3 meaningful commits on real code  
- [ ] Exercise 02 — Understand `diff` vs `diff --staged`  
- [ ] Exercise 03 — Fix a bad commit using `--amend`  

---

## ✅ Completion Checklist

- [ ] I understand the difference between working directory, staging area, and repository  
- [ ] I used `git status` before and after actions  
- [ ] My commits have clear and meaningful messages  
- [ ] I completed all exercises  
- [ ] I wrote my own notes in `GITLOG.md`  

---

## 📝 Notes

This module focuses on building muscle memory with Git.
The goal is not just to run commands, but to understand
what happens at each step.